# 客户订单管理系统 - 面试答案与说明

## 项目结构

```
qince/
├── database/          # 数据库脚本
│   ├── schema.sql     # 建表脚本（PostgreSQL）
│   └── data.sql       # 测试数据
├── backend/           # Spring Boot 后端
│   ├── pom.xml
│   └── src/main/java/com/example/orderms/
│       ├── config/    # 配置类（Security/CORS/MyBatis-Plus）
│       ├── common/    # 公共类（Result/异常处理）
│       ├── entity/    # 数据库实体
│       ├── mapper/    # MyBatis Mapper 接口
│       ├── dto/       # 数据传输对象（请求体/响应体）
│       ├── service/   # 业务逻辑层
│       ├── controller/# 接口控制器
│       └── security/  # JWT 工具类/过滤器
├── frontend/          # React 前端
│   └── src/
│       ├── store/     # Redux 状态管理
│       ├── api/       # Axios 接口调用
│       ├── pages/     # 页面组件
│       ├── components/# 可复用组件
│       ├── router/    # 路由配置（含路由守卫）
│       └── types/     # TypeScript 类型定义
└── 面试答案与说明.md   # 本文件
```

---

## 启动说明

### 前置条件
1. PostgreSQL 15+ 运行在 localhost:5432，数据库名 `order_ms`
2. Redis 运行在 localhost:6379
3. JDK 17+，Maven 3.8+
4. Node.js 18+

### 数据库初始化
```sql
CREATE DATABASE order_ms;
\c order_ms
\i database/schema.sql
\i database/data.sql
```

### 启动后端
```bash
cd backend
mvn spring-boot:run
# 后端运行在 http://localhost:8080
```

### 启动前端
```bash
cd frontend
npm install
npm run dev
# 前端运行在 http://localhost:3000
```

### 测试账号
| 账号     | 密码       | 角色     |
|----------|------------|----------|
| admin    | Admin123!  | 管理员   |
| operator | Admin123!  | 操作员   |

---

## AI 辅助标注说明

本项目全程使用 Claude AI 辅助，以下是主要辅助场景和自身修改逻辑：

### 后端
| 代码位置 | AI 辅助内容 | 自身修改/优化 |
|---------|------------|-------------|
| `SecurityConfig.java` | Spring Security 6.x 新 API 结构（Lambda DSL） | 调整了权限规则和 CORS 配置 |
| `JwtAuthFilter.java` | JWT 过滤器骨架 | 增加了 Redis 黑名单检查逻辑 |
| `OrderMapper.xml` | 动态 SQL `<if>` 结构 | 修复了 PostgreSQL `ILIKE`（不区分大小写模糊查询，Oracle 用 `UPPER(LIKE)` 不同）|
| `OrderServiceImpl.java` | 订单状态流转逻辑骨架 | 补充了业务规则细节（禁止跨状态跳转）|

### 前端
| 代码位置 | AI 辅助内容 | 自身修改/优化 |
|---------|------------|-------------|
| `orderSlice.ts` | createAsyncThunk 模板 | 增加了客户/设备下拉数据缓存逻辑 |
| `axios.ts` | 请求重试机制 | 调整了重试条件（只重试 5xx，不重试 4xx）|
| `OrdersPage` | Table 列定义结构 | 增加了角色权限控制（isAdmin 判断）|
| 客户-设备联动 | Select onSearch 触发后端搜索 | 优化了防抖（避免每次输入都请求） |

---

## 一、前端模块拓展问题

### 1. useMemo 和 useCallback 的区别及本项目使用场景

- **useMemo**：缓存**计算结果**（值），当依赖不变时跳过重新计算。
  - 本项目应用：OrdersPage 中的 `STATUS_COLOR`/`STATUS_LABEL` 映射对象如果是动态生成的，可以用 useMemo 缓存。
  
- **useCallback**：缓存**函数引用**，当依赖不变时返回同一个函数对象，防止子组件不必要的重渲染。
  - 本项目应用：`reload` 函数传递给 `OrderFormModal` 的 `onSuccess` 回调时，用 `useCallback` 包裹，避免每次父组件渲染时子组件都重新渲染。

```tsx
// 本项目 OrdersPage 中的实际应用
const reload = useCallback((newParam?: Partial<OrderQueryParam>) => {
  const merged = { ...queryParam, ...newParam }
  dispatch(setQueryParam(merged))
  dispatch(fetchOrders(merged))
}, [queryParam, dispatch])  // 只有 queryParam 或 dispatch 变化时才重新生成函数
```

### 2. Redux Toolkit 如何实现不同业务模块的状态隔离

- 每个业务模块创建独立的 `createSlice`（如 `authSlice`、`orderSlice`）
- 在 `configureStore` 的 `reducer` 字段中用不同的 key 注册，每个 key 对应一块独立的状态空间
- 通过 `useAppSelector(s => s.order.xxx)` 精确访问某模块状态，不会串扰其他模块
- 本项目将订单的列表数据、弹窗状态、查询参数都隔离在 `order` slice 中，登录信息独立在 `auth` slice 中

### 3. 如何优化前端下拉联动（客户-设备）的加载性能

1. **本地缓存**（已实现）：Redux Store 缓存客户/设备列表，只有第一次打开弹窗时请求，之后复用缓存
2. **搜索防抖**：对 Select 的 `onSearch` 加防抖（300ms），避免每次按键都触发请求
3. **虚拟滚动**：当选项超过 500 条时，使用 `rc-virtual-list` 替代普通 Select，只渲染可见区域
4. **懒加载**：设备列表较大时可改为按需加载——先不加载，等用户打开弹窗时才请求

---

## 二、后端模块拓展问题

### 数据校验说明

使用 JSR380（`jakarta.validation`）注解：
- `@NotNull` / `@NotBlank`：必填校验
- `@DecimalMin(value="0")`：金额非负
- `@Pattern(regexp="PURCHASE|SALE")`：枚举值校验
- 自定义业务校验在 Service 层手动实现：交货日期、客户+设备组合重复

### Spring Security + JWT 认证流程

```
前端请求
  ↓ Authorization: Bearer <token>
JwtAuthFilter（每次请求执行）
  → 检查 Redis 黑名单
  → 验证 JWT 签名和过期时间
  → 解析用户名+角色，放入 SecurityContext
  ↓
Controller（@PreAuthorize("hasRole('ADMIN')")）
  → 从 SecurityContext 获取角色，判断权限
```

---

## 三、数据库模块

### PostgreSQL 与 Oracle 主要差异（面试重点）

| 特性 | PostgreSQL | Oracle |
|------|-----------|--------|
| 自增主键 | `BIGSERIAL` / `GENERATED ALWAYS AS IDENTITY` | `SEQUENCE` + `TRIGGER` 或 `IDENTITY` |
| 字符串模糊查询 | `LIKE`（区分大小写）/ `ILIKE`（不区分） | `LIKE`（配合 `UPPER()`）|
| 当前时间 | `NOW()` / `CURRENT_TIMESTAMP` | `SYSDATE` |
| 字符串拼接 | `\|\|` 或 `CONCAT()` | `\|\|` 或 `CONCAT()` |
| 分页 | `LIMIT n OFFSET m` | `ROWNUM` 或 `FETCH FIRST n ROWS ONLY`（12c+）|
| 日期截断 | `DATE(timestamp)` | `TRUNC(date)` |
| 事务隔离 | 默认读已提交 | 默认读已提交（相同）|

### Redis 缓存逻辑

- **用途1（已实现）**：JWT 黑名单——退出登录后将 token 存入 Redis，过期时间与 JWT 一致（24h）
- **用途2（可拓展）**：热点订单详情缓存——高频访问的详情页 key 为 `order:detail:{id}`，TTL 5分钟，更新订单时删除对应 key

---

## 四、联调与问题排查

### 模拟场景1：前端提交订单返回400"客户+设备组合当日已存在"，但前端未显示该订单

**排查步骤（AI 辅助分析）：**
1. 检查前端查询参数：是否筛选条件过滤掉了这条订单（如状态筛选）
2. 检查 Redux 缓存：订单新增后是否触发了列表重新请求（`onSuccess` 中调用 `reload()`）
3. 检查后端查询逻辑：`countDuplicateOrder` 中 `DATE(create_time) = CURRENT_DATE` 是否有时区问题
4. 检查 PostgreSQL 时区配置：`SHOW timezone;` 确认与应用服务器一致

**解决方案：**
- 前端 `OrdersPage.handleSubmit` 成功后强制调用 `reload({ page: 1 })` 回到第一页
- 后端时区统一设置：`spring.datasource.url` 末尾加 `?TimeZone=Asia/Shanghai`

### 模拟场景2：订单列表无法显示设备名称（只显示设备ID）

**排查步骤（AI 辅助分析）：**
1. 查看 SQL 执行日志（`log-impl: StdOutImpl` 已配置），确认 JOIN 语句是否正确执行
2. 检查 `OrderMapper.xml` 的 `resultMap` 中是否有 `equipment_name` → `equipmentName` 的映射
3. 检查 PostgreSQL 字段类型：设备名称是 `VARCHAR`，MyBatis 应能自动映射，排除类型不兼容
4. 检查前端：`Order` 类型定义中是否有 `equipmentName` 字段，Table columns 中是否正确引用

**根因**：最常见是 `resultMap` 中漏写了 `equipment_name` 列映射，或 LEFT JOIN 的 ON 条件写错导致关联失败。

---

## 五、综合拓展

### AI 工具使用总结

1. **接口框架代码**：Spring Security、JWT 配置的模板代码由 AI 生成，减少查文档时间约 60%
2. **PostgreSQL 适配**：AI 提示了 `ILIKE`（不区分大小写）代替 Oracle 的 `UPPER(LIKE)`，`BIGSERIAL` 代替 Oracle `SEQUENCE`，以及 `DATE()` 函数代替 Oracle `TRUNC()`
3. **React 状态管理**：Redux Toolkit 的 `createAsyncThunk` 模板由 AI 生成，自身在此基础上增加了缓存判断逻辑
4. **业务逻辑漏洞发现**：AI 提示"编辑时重复订单校验要排除自身 ID"，这是容易遗漏的细节

### 优化建议

1. **性能优化 - 订单批量导入**  
   业务场景：销售团队经常需要从 Excel 导入大批量订单  
   实现思路：前端使用 `xlsx` 库解析 Excel，后端提供 `POST /api/orders/import` 接口接收数组，Service 层用 MyBatis-Plus 的 `saveBatch()` 批量插入，数据库开启 `reWriteBatchedInserts=true` PostgreSQL 参数提升批量写入性能

2. **安全性 - 敏感操作审计日志**  
   业务场景：监管合规要求记录谁在什么时间对哪个订单做了什么操作  
   实现思路：Spring AOP 切面拦截所有 `@PostMapping`/`@PutMapping`/`@DeleteMapping`，记录操作人、时间、操作类型、变更前后数据到 `audit_log` 表，同时异步写入 Redis 做近期操作缓存

3. **可扩展性 - 订单状态机重构**（进阶）  
   当前状态流转用 switch-case，扩展新状态时需改多处  
   建议用状态机模式：定义 `OrderStateMachine`，用 Map 存储 `当前状态 → 允许的操作 → 目标状态`，新增状态只需加一条配置
